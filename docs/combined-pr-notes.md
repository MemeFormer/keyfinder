# Combined PR notes

This branch intentionally combines two closely related updates:

1. AnalysisLogView UX polish (adds a close button and related behavior).
2. App version metadata update (targeting 1.9) to ship the UI improvement together.

These are bundled so release metadata and user-facing behavior land together in one review/deploy step.
